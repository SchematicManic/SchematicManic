const axios = require('axios');

const callAutoForge = async (params) => {
  //given parameters, make API call to Autoforge

  //return file (or file promise)
  return params; //delete me
};

module.exports = callAutoForge;